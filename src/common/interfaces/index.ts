export enum ResponseMessage {
  SUCCESS = 'Request Successful',
  FAILED = 'Request Failed',
}

export enum Gender {
  MALE = 'Male',
  FEMALE = 'Female',
  OTHER = 'Other',
}

export enum ApiTag {
  GROUP = 'Group',
  EVENT = 'Event',
}
